//A shape function to generate straight-line node sequences.

module.exports = function(node, grid) {
	//node should be the key of a specific node in the grid.nodes array.
	//This should be how all shapes start.
	
}
	